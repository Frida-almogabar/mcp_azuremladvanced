# Azure ML Configuration
AZURE_SUBSCRIPTION_ID=your-subscription-id
AZUREML_RESOURCE_GROUP=your-resource-group
AZUREML_WORKSPACE_NAME=your-workspace-name

# Optional: Service Principal credentials (for local development)
# Leave empty to use managed identity in Azure or Azure CLI locally
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
AZURE_TENANT_ID=

# MCP Server Configuration
MCP_SERVER_HOST=0.0.0.0
MCP_SERVER_PORT=8080
